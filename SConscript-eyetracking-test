Import('env')

etEnv = env.Clone()

etEnv.CompilationDatabase()
etEnv.Append(
  CPPPATH=[
    'eyetracking',
    '/usr/include/eigen3',
  ]
)
etEnv.Program(
  target = '../bin/eyetracking-test',
  source = Glob('eyetracking/*.cpp') + Glob('eyetracking/*.cu') + Glob('eyetracking/SingleEyeFitter/*.cpp') + ['eyetracking/test/eyetracking-test.cpp'],
  LIBS=['stdc++', 'pthread', 'rt', 'opencv_core', 'opencv_imgproc', 'opencv_videoio', 'opencv_highgui', 'cuda', 'cudla', 'cudart', 'boost_thread', 'ceres', 'glog'],
)


